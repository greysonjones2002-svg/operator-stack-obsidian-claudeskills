---
type: wiki-index
updated:
---

# Knowledge Wiki Index

Catalog of everything in this knowledge base. `source-ingest` updates this on every new source. `wiki-query` reads this file first before drilling into individual pages — keep it current or queries get worse.

## How to read this

Each entry: link to the page, one-line summary, category, and the date it was last touched. Grouped by category below; add categories as the wiki grows, don't force everything into a fixed taxonomy.

## Overview / synthesis pages

*(High-level pages that pull together what the wiki knows about a topic as a whole — these get updated most often as new sources come in.)*

## Entities

*(People, companies, products — anything that's a "thing" the wiki tracks over time.)*

## Concepts

*(Ideas, frameworks, recurring themes that show up across multiple sources.)*

## Sources

*(One page per ingested source, if the source itself warrants a dedicated page beyond what's folded into entity/concept pages.)*

## Comparisons

*(Side-by-side pages — competitor comparisons, option analyses, before/after — anything that's inherently relational rather than about one thing.)*
