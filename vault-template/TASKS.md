# Tasks

Living task list. Claude reads this at the start of every session and updates it as work happens — new commitments get added, completed items get checked off, stale items get flagged instead of silently dropped.

## Format

```
- [ ] Task description — context if needed (due: date, client: [[Name]])
```

## Today / this week

- [ ] *(empty — this fills in as you work with Claude)*

## Waiting on someone else

## Backlog / someday

## Recently completed

*Completed tasks move here for a week before archiving, so recent history stays visible.*
